# Frontend Dist Policy

Skyforge embeds portal build artifacts under server distribution paths for deployment.

Policy:

- frontend artifact updates must be generated by build tooling,
- avoid manual edits of hashed asset files,
- CI should detect stale or duplicate hashed entrypoint artifacts.
